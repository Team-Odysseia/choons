spring.application.name=choons

# Porta do Servidor
server.port=8080

# Ativa��o das Virtual Threads (JDK 21+)
spring.threads.virtual.enabled=true

# Configura��o do Banco de Dados PostgreSQL
spring.datasource.url=${POSTGRES_URI}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASS}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Limites de Upload (Importante para arquivos de �udio)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Configura��es do Cloudflare R2
r2.endpoint=https://e3f4cb7133481466fcd4d8f89f377173.r2.cloudflarestorage.com
r2.bucket-name=choons
r2.access-key=${R2_ACCESS_KEY}
r2.secret-key=${R2_SECRET_KEY}

# JWT
jwt.secret=${JWT_SECRET:LYqyfJlfJhOiIzum61lUAdHAJVYzbdi8kxn9c1UiLcB}